rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Helper: Verifica se o usuário está autenticado
    function isAuthenticated() {
      return request.auth != null;
    }

    // Helper: Verifica se o usuário logado é um administrador de forma direta
    function isAdmin() {
      return isAuthenticated() && 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }
    
    // Helper: Verifica se o usuário logado está aprovado
    function isApproved() {
      return isAuthenticated() && (
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin' || 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isApproved == true
      );
    }

    // USERS: Regras para a coleção de usuários
    match /users/{userId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() && request.auth.uid == userId;
      allow update: if isAuthenticated() && (request.auth.uid == userId || isAdmin());
      
      // EXCLUSÃO: Permissão absoluta para administradores
      allow delete: if isAuthenticated() && 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }
    
    // SETTINGS: Configurações globais
    match /settings/{settingId} {
      allow read: if isAuthenticated();
      allow write: if isAdmin();
    }
    
    // SETLISTS: Repertórios
    match /setlists/{setlistId} {
      allow read: if isAuthenticated() && (
        isAdmin() || 
        isApproved() || 
        resource.data.isPublic == true || 
        resource.data.isVisible == true || 
        resource.data.creatorId == request.auth.uid
      );
      
      allow write: if isAuthenticated() && (
        isAdmin() || 
        isApproved() || 
        (exists(resource) && resource.data.creatorId == request.auth.uid) ||
        (!exists(resource) && request.resource.data.creatorId == request.auth.uid)
      );
    }

    // CATCH-ALL: Outras coleções
    match /{collection}/{docId} {
      allow read: if isAuthenticated();
      allow write: if isApproved();
    }
  }
}